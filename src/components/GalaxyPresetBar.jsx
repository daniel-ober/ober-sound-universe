// src/components/GalaxyPresetBar.jsx
import "./GalaxyPresetBar.css";

/**
 * DEPRECATED
 * GalaxyPresetBar UI has been migrated into TopBar → MASTER dropdown.
 * This component intentionally renders nothing.
 *
 * Safe to delete once all imports/usages are removed.
 */
export function GalaxyPresetBar() {
  return null;
}